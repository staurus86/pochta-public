---
phase: 10-audit-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - scripts/baselines/baseline_v1.json
  - data/audit_sample_50_report.json
  - .gitignore
autonomous: false
nyquist_compliant: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
must_haves:
  truths:
    - "`scripts/baselines/baseline_v1.json` exists and is committed to git"
    - "`scripts/baselines/baseline_v1.json` contains per-field metrics for all 7 fields, n8n_signal section, and raw_message_ids list"
    - "`data/audit_sample_50_report.json` exists locally (gitignored) and contains 50 annotated-ready rows"
    - "Baseline numbers are statistically plausible (no field shows 0% or 100%, all in 1-99% range)"
    - "Reviewer (human in checkpoint) has eyeballed the report and confirms numbers match expectations"
  artifacts:
    - path: scripts/baselines/baseline_v1.json
      provides: "Persistent v1.1 baseline metrics for delta comparisons in phases 11-14"
      contains: "version"
    - path: data/audit_sample_50_report.json
      provides: "Manual audit input — 50 rows for human review"
      contains: "rows"
    - path: .gitignore
      provides: "Ensures data/audit_sample_50_report.json is NOT committed (raw email content)"
  key_links:
    - from: scripts/baselines/baseline_v1.json
      to: phases 11-14 delta scripts
      via: "Phases 11-14 plans will read this file and compute delta"
      pattern: "baseline_v1\\.json"
---

<objective>
Wave-2 wiring plan: run both audit scripts against live production, persist `scripts/baselines/baseline_v1.json` as the canonical v1.1 baseline, run the 50-email script and save the structured report, then have a human eyeball the numbers before committing the baseline to git.

Purpose: A script that compiles but never runs against production is not a baseline. This plan turns the deliverables of Plans 01 and 02 into a measured baseline file that subsequent phases will compute deltas against.

Output:
- `scripts/baselines/baseline_v1.json` (committed to git)
- `data/audit_sample_50_report.json` (gitignored — raw email content)
- `.gitignore` updated to exclude `data/audit_sample_50_report.json`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-audit-baseline/10-RESEARCH.md
@.planning/phases/10-audit-baseline/10-VALIDATION.md
@.planning/phases/10-audit-baseline/10-01-PLAN.md
@.planning/phases/10-audit-baseline/10-02-PLAN.md
@CLAUDE.md
@.gitignore

<interfaces>
<!-- Production credentials (already used in scripts/_audit_100.py — public in repo per existing pattern). -->
- API: https://pochta-production.up.railway.app
- Login: admin / LgxaZ@ZDgNBXgSpnmTHEW6MC

<!-- Expected baseline shape (from Plan 01, Task 3): -->
{
  "version": 1,
  "created_at": "<ISO Z>",
  "sample_size": 300,
  "sample_seed": 42,
  "projects": ["project-3-mailbox-file", "project-4-klvrt-mail"],
  "source": "live-prod",
  "fields": { "fio": {present, noise_free, n}, "inn": {...}, ... 7 fields total },
  "n8n_signal": { "total_with_feedback", "approved", "needs_rework", "not_reviewed", "in_sample_with_feedback" },
  "raw_message_ids": [ "...", "...", ... 300 ids ]
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update .gitignore to exclude raw 50-email report; ensure scripts/baselines/ is tracked</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current content — must not duplicate existing rules)
  </read_first>
  <action>
    Append to `.gitignore` (if not already present):

    ```
    # Phase 10 — audit report contains raw email content, do not commit
    data/audit_sample_50_report.json
    ```

    Do NOT add `scripts/baselines/` to .gitignore — the baseline JSON is intentionally committed so phases 11-14 can read it.

    Verification step: confirm `scripts/baselines/` is NOT mentioned anywhere in `.gitignore`. If a prior rule like `scripts/baselines/` or `baselines/` exists, remove it (this plan's intent is the baseline IS committed).
  </action>
  <verify>
    <automated>grep -c "data/audit_sample_50_report.json" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data/audit_sample_50_report.json" .gitignore` returns at least 1
    - `grep -c "^scripts/baselines" .gitignore` returns 0
    - `grep -c "^baselines/" .gitignore` returns 0
  </acceptance_criteria>
  <done>.gitignore excludes the raw 50-email report but does NOT exclude scripts/baselines/.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run audit_baseline.py live against production and write scripts/baselines/baseline_v1.json</name>
  <files>scripts/baselines/baseline_v1.json</files>
  <read_first>
    - scripts/audit_baseline.py (created by Plan 01)
    - .planning/phases/10-audit-baseline/10-RESEARCH.md (Pitfall 5 — n8n SSL; use server endpoint)
  </read_first>
  <action>
    Execute this command from project root (Windows PowerShell or bash, both work — Python is the same):

    ```
    python scripts/audit_baseline.py --limit 300 --out scripts/baselines/baseline_v1.json
    ```

    This will:
    1. Call `login()` against prod (gets admin Bearer token).
    2. Fetch messages from both `project-3-mailbox-file` and `project-4-klvrt-mail`.
    3. Filter to `classification.label == 'Клиент'`.
    4. Sample 300 with `random.seed(42)`.
    5. Compute per-field present% and noise_free% across 7 fields.
    6. Fetch n8n feedback via `GET /api/admin/validation-feedback?limit=500` using the same token.
    7. Write `scripts/baselines/baseline_v1.json`.

    Expected console output includes lines like:
    - `Loaded N total messages from live-prod`
    - `Filtered to N_client Клиент-class`
    - `Sampled 300 (seed=42)`
    - A table with 7 rows (fio, inn, phone, article, brand, qty, product_name)
    - `Wrote baseline: scripts/baselines/baseline_v1.json`

    **Plausibility expectations** (from RESEARCH.md and project memory, May 2026 production):
    - fio.present% expected ~70-85% (memory: ~72%)
    - inn.present% expected ~40-55%
    - phone.present% expected ~45-65%
    - article.present% expected ~40-55% (memory: 43%)
    - brand.present% expected ~50-65% (memory: 58%)
    - qty.present% expected ~30-50%
    - product_name.present% expected ~25-45%

    If any field shows 0% or 100% — STOP and investigate (likely indicates a bug in Plan 01 scorer or the live data shape changed). Do NOT silently accept anomalies.

    **Fallback**: if the production API is unreachable or login fails, fall back to:
    ```
    python scripts/audit_baseline.py --local --limit 300 --out scripts/baselines/baseline_v1.json
    ```
    and document `source: "local:..."` in the resulting baseline (the script does this automatically). Note in the final SUMMARY that baseline came from snapshot, not live.
  </action>
  <verify>
    <automated>python -c "import json; b=json.load(open('scripts/baselines/baseline_v1.json',encoding='utf-8')); assert b['version']==1; assert set(b['fields'].keys())=={'fio','inn','phone','article','brand','qty','product_name'}; assert all(0 < b['fields'][f]['present'] < 1 for f in b['fields']); assert 'n8n_signal' in b; assert isinstance(b['raw_message_ids'], list) and len(b['raw_message_ids']) >= 30; print('OK', b['source'], b['sample_size'])"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/baselines/baseline_v1.json` exists
    - `python -c "import json; b=json.load(open('scripts/baselines/baseline_v1.json',encoding='utf-8')); assert b['version']==1"` exits 0
    - All 7 field present% values are strictly between 0.0 and 1.0 (exclusive — not 0%, not 100%)
    - `b['sample_size'] >= 30` (real production sample, not a 5-row smoke test)
    - `b['raw_message_ids']` is a non-empty list with length equal to `sample_size`
    - `b['source']` is either `"live-prod"` or starts with `"local:"`
    - `b['n8n_signal']` dict is present with key `total_with_feedback`
  </acceptance_criteria>
  <done>Baseline file written with realistic per-field metrics in 0.01-0.99 range, n8n signal section populated, and ≥30 message IDs persisted for delta computation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run audit_sample_50.py live and write data/audit_sample_50_report.json</name>
  <files>data/audit_sample_50_report.json</files>
  <read_first>
    - scripts/audit_sample_50.py (created by Plan 02)
    - .planning/phases/10-audit-baseline/10-RESEARCH.md (open question 2 — manual audit process)
  </read_first>
  <action>
    Execute from project root:

    ```
    python scripts/audit_sample_50.py --limit 50 --out data/audit_sample_50_report.json
    ```

    Expected:
    - Fetches from prod (or local if live fails — same fallback rule as Task 2).
    - Filters to Клиент-class.
    - Samples 50 deterministically.
    - Builds one row per message with 7 fields each `{value, present, noise, noise_reasons[]}`.
    - Writes the JSON report.

    Print a 7-line summary table after the run (the script already does this) showing for each field: how many of 50 rows are `present=True` and how many are `noise=True`.

    **Fallback**: if live API unreachable, use `--local --limit 50`. The report's `source` field documents which path was taken.
  </action>
  <verify>
    <automated>python -c "import json; r=json.load(open('data/audit_sample_50_report.json',encoding='utf-8')); assert r['version']==1; assert r['sample_seed']==42; assert isinstance(r['rows'], list); assert len(r['rows']) >= 10; row=r['rows'][0]; assert set(row['fields'].keys())=={'fio','inn','phone','article','brand','qty','product_name'}; print('OK rows=', len(r['rows']))"</automated>
  </verify>
  <acceptance_criteria>
    - File `data/audit_sample_50_report.json` exists
    - `r['version'] == 1` and `r['sample_seed'] == 42`
    - `r['rows']` length >= 10 (allows fallback to smaller local samples in CI; live target is 50)
    - Each row has all 7 fields with `{value, present, noise, noise_reasons}` keys
    - File is NOT staged for git commit (covered by .gitignore from Task 1)
  </acceptance_criteria>
  <done>50-email structured audit report written. Each row contains the raw extracted values + auto-flagged noise reasons, ready for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human plausibility review of baseline + 50-email report</name>
  <files>scripts/baselines/baseline_v1.json, data/audit_sample_50_report.json</files>
  <read_first>
    - scripts/baselines/baseline_v1.json
    - data/audit_sample_50_report.json
  </read_first>
  <what-built>
    Two artifacts have been produced:
    1. `scripts/baselines/baseline_v1.json` — automated per-field accuracy on a 300-sample of Клиент-class production emails, plus n8n feedback signal.
    2. `data/audit_sample_50_report.json` — structured per-row audit data for 50 emails with auto-flagged noise reasons in each field.
  </what-built>
  <action>
    Pause for human review. Print to the executor's console:

    "Phase 10 baseline and 50-email report have been written. Before committing, please review the artifacts and confirm plausibility."

    The human reviewer will:
    1. Open `scripts/baselines/baseline_v1.json` and confirm `version=1`, `sample_size>=30`, `sample_seed=42`. Check each of the 7 fields has present% in plausible range (Task 2 expectations: fio ~70-85%, inn ~40-55%, phone ~45-65%, article ~40-55%, brand ~50-65%, qty ~30-50%, product_name ~25-45%). Check `n8n_signal.total_with_feedback >= 0` (0 is acceptable). Confirm `raw_message_ids` is non-empty.
    2. Open `data/audit_sample_50_report.json` and spot-check 3-5 rows: do `fio.value` look like real names? Do `article.value` look like real codes? Do `noise_reasons` flag the obvious garbage?
    3. Optionally fill in `reviewer_notes` on surprising rows (one sentence each).
    4. If numbers look implausible (e.g., article.present is 5%, way below expected 40-55%), STOP and investigate.

    Wait for the resume signal before proceeding to Task 5 (commit).
  </action>
  <how-to-verify>
    1. Open `scripts/baselines/baseline_v1.json`:
       - Confirm `version=1`, `sample_size>=30`, `sample_seed=42`.
       - Check that each of the 7 fields has present% in plausible range (see Task 2 expectations).
       - Check `n8n_signal.total_with_feedback` is >= 0 (0 is acceptable — only 10-20 emails have been sent to n8n historically).
       - Confirm `raw_message_ids` is a non-empty list.

    2. Open `data/audit_sample_50_report.json`:
       - Spot-check 3-5 rows. For each:
         - Does the `fio.value` look like a real person name? If `noise=true`, do the reasons make sense (e.g., "contains-org-keyword" for "ООО Ромашка")?
         - Does the `article.value` list look like real article codes, or garbage? Do `noise_reasons` flag the garbage correctly?
         - Are there any rows where the auto-flag misses an obvious problem? Note those in `reviewer_notes`.

    3. Optional: fill in `reviewer_notes` field on rows with surprising or wrong detections (single sentence per row is enough).

    4. If the baseline numbers look implausible (e.g., article.present is 5% — way below the expected 40-55%), STOP and investigate before proceeding to Phase 11. The whole point of this phase is a TRUSTABLE baseline.
  </how-to-verify>
  <verify>Manual review — checkpoint pauses execution. No automated command; resume signal from the user is the gate.</verify>
  <acceptance_criteria>
    - User has manually opened both files
    - User confirms baseline numbers are within expected ranges
    - User has the option to (but is not required to) annotate `reviewer_notes`
    - User explicitly approves via resume signal
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to commit, "rerun" to re-run the scripts, or describe issues (e.g., "article.present% looks too low — investigate").</resume-signal>
  <done>Human approval recorded; proceed to commit task.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Commit scripts/baselines/baseline_v1.json + scripts + .gitignore to git</name>
  <files>scripts/baselines/baseline_v1.json, scripts/audit_baseline.py, scripts/audit_sample_50.py, .gitignore</files>
  <read_first>
    - .gitignore (confirm Task 1 changes present)
    - scripts/baselines/baseline_v1.json (confirm Task 2 wrote it)
  </read_first>
  <action>
    After human approval in Task 4, commit the Phase 10 deliverables.

    Stage the four files explicitly (NOT `git add -A`):

    ```
    git add scripts/audit_baseline.py scripts/audit_sample_50.py scripts/baselines/baseline_v1.json .gitignore
    ```

    Then create a commit using a HEREDOC for proper formatting:

    ```
    git commit -m "$(cat <<'EOF'
    feat(audit): Phase 10 — per-field baseline + 50-email audit scripts

    AUDIT-01: scripts/audit_sample_50.py — structured 50-email manual audit report.
    AUDIT-02: scripts/audit_baseline.py — per-field % across 7 detection fields.
    AUDIT-03: n8n manager feedback fetched via GET /api/admin/validation-feedback.

    Persistent baseline: scripts/baselines/baseline_v1.json — used by phases 11-14
    to compute deltas. Sample is deterministic (seed=42); raw_message_ids persisted
    so future delta runs can target the same set.

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    EOF
    )"
    ```

    Verify commit landed:
    ```
    git log -1 --stat
    ```

    Do NOT:
    - Push to remote (only if user explicitly asks)
    - Commit `data/audit_sample_50_report.json` (covered by .gitignore)
    - Use `--no-verify` flags
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" -- scripts/baselines/baseline_v1.json scripts/audit_baseline.py scripts/audit_sample_50.py | grep -c "Phase 10"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:"%s"` returns a subject line containing "Phase 10" or "AUDIT"
    - `git ls-files scripts/baselines/baseline_v1.json` returns the file (it is tracked)
    - `git ls-files scripts/audit_baseline.py` returns the file
    - `git ls-files scripts/audit_sample_50.py` returns the file
    - `git ls-files data/audit_sample_50_report.json` returns NOTHING (correctly gitignored)
    - `git status --porcelain` shows no `scripts/baselines/baseline_v1.json` line (it's committed, not modified)
  </acceptance_criteria>
  <done>Single git commit lands containing both new scripts, the baseline JSON, and the gitignore update. Raw 50-email report stays local.</done>
</task>

</tasks>

<verification>
- `git ls-files scripts/baselines/baseline_v1.json` lists the file (tracked in git)
- `python -c "import json; b=json.load(open('scripts/baselines/baseline_v1.json',encoding='utf-8')); assert b['version']==1"` exits 0
- `python -c "import json; r=json.load(open('data/audit_sample_50_report.json',encoding='utf-8')); assert len(r['rows']) >= 10"` exits 0
- `git ls-files data/audit_sample_50_report.json` lists NOTHING (file is gitignored)
- User has approved plausibility via checkpoint
</verification>

<success_criteria>
- AUDIT-01: 50-email structured bug report exists at `data/audit_sample_50_report.json` with the row schema from Plan 02
- AUDIT-02: `scripts/baselines/baseline_v1.json` committed to git with all 7 per-field metrics in plausible ranges
- AUDIT-03: n8n signal section present in baseline (can be 0 if no feedback yet — that's a valid observation, not a failure)
- Phase 10 goal achieved: phases 11-14 can re-run `python scripts/audit_baseline.py --limit 300` and compute deltas against the committed baseline
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-baseline/10-03-SUMMARY.md`
</output>
