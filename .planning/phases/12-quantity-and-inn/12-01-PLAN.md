---
phase: 12-quantity-and-inn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/batch-12-fixes.test.js
  - src/services/email-analyzer.js
  - src/services/integration-api.js
  - src/services/crm-adapters.js
  - src/services/integration-openapi.js
  - src/services/llm-extractor.js
  - .railway-deploy/src/services/email-analyzer.js
  - .railway-deploy/src/services/integration-api.js
  - .railway-deploy/src/services/crm-adapters.js
  - .railway-deploy/src/services/llm-extractor.js
autonomous: true
requirements: [ART-04]

must_haves:
  truths:
    - "positions equals the count of unique normalized article codes in a lead"
    - "totalQty equals the sum of qty across unique positions (null qty counts as 0)"
    - "Belgormash case (2 articles x 5 qty, 18 duplicated lineItems) yields positions: 2, totalQty: 10"
    - "positions is 0 (not undefined) when a lead has no articles"
    - "CRM and integration payloads carry the deduplicated positions value, not raw lineItems.length"
  artifacts:
    - path: "tests/batch-12-fixes.test.js"
      provides: "Phase 12 regression test file (Belgormash positions/totalQty + INN checksum stubs)"
      contains: "finalizeLeadCounts"
    - path: "src/services/email-analyzer.js"
      provides: "finalizeLeadCounts() post-processing + positions/totalQty fields"
      contains: "function finalizeLeadCounts"
    - path: "src/services/integration-api.js"
      provides: "serialization of positions + total_qty to storage"
      contains: "lead.positions"
  key_links:
    - from: "src/services/email-analyzer.js analyzeEmail()"
      to: "finalizeLeadCounts(lead)"
      via: "single call after validateSenderFields (line ~1807)"
      pattern: "finalizeLeadCounts\\(lead\\)"
    - from: "src/services/integration-api.js"
      to: "stored payload"
      via: "positions/total_qty serialization"
      pattern: "positions: lead.positions"
    - from: "src/services/crm-adapters.js"
      to: "CRM comment line"
      via: "lead.positions read"
      pattern: "lead.positions"
---

<objective>
Fix position/quantity overcounting (ART-04). Replace the seven scattered `lead.totalPositions = Math.max(lineItems.length, articles.length)` assignments — which count duplicated lineItems across extraction zones (Belgormash: 18 entries for 2 real articles) — with a single authoritative post-processing step `finalizeLeadCounts(lead)` that deduplicates by `normalizeArticleCode` and produces `lead.positions` (unique article-code count) and `lead.totalQty` (sum of qty over unique positions).

Purpose: Belgormash and similar multi-zone emails currently report inflated position/qty counts in the CRM payload. After this fix `positions` reflects real unique articles and `totalQty` reflects real summed quantity.
Output: `finalizeLeadCounts()` in `email-analyzer.js`, new `lead.positions`/`lead.totalQty` fields, updated serialization + consumers, `tests/batch-12-fixes.test.js` with the Belgormash regression and a no-articles case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-quantity-and-inn/12-CONTEXT.md
@.planning/phases/12-quantity-and-inn/12-RESEARCH.md
@.planning/phases/12-quantity-and-inn/12-VALIDATION.md

<interfaces>
<!-- Extracted from codebase. Use directly — no exploration needed. -->

From src/services/email-analyzer.js:
- `function normalizeArticleCode(value)` at line 5746 — returns normalized article-code string (collapses spaces/dashes). Use for dedup key.
- `export function analyzeEmail(project, payload)` at line 603; `return result` at line 1871.
- `const senderCorrections = validateSenderFields(sender);` at line 1807 — this is the LAST merge/normalize step before result is finalized. Insert `finalizeLeadCounts(lead)` immediately AFTER this line.
- Current `lead.totalPositions` writes (all to be left as intermediate trackers, but the FINAL authoritative value is set by finalizeLeadCounts): lines 1059, 1089, 1134, 1448, 1562, 1746, 3064, 3183.
- `lead.lineItems` entries have shape `{ article, quantity, unit, description / descriptionRu }`. `quantity` may be `null`.
- `lead.articles` is a string[] of article codes.

From src/services/integration-api.js (line 138-141) — this is where camelCase lead is serialized to snake_case for storage:
```javascript
lead: {
  request_type: lead.requestType || null,
  free_text: lead.freeText || "",
  total_positions: lead.totalPositions || 0,   // <-- change to: positions: lead.positions || 0, total_qty: lead.totalQty || 0
  articles: lead.articles || [],
  ...
```

From src/services/crm-adapters.js — reads STORED snake_case lead:
- line 133: `` `Позиций: ${lead.total_positions || 0}`, `` (inside COMMENTS array of buildBitrix-style payload)
- line 167: `totalPositions: lead.total_positions || 0,` (inside build1CPayload request object)

From src/services/llm-extractor.js line 329:
`result.lead.totalPositions = (result.lead.lineItems || []).length || (result.lead.articles || []).length;`

From src/services/integration-openapi.js line 964:
`total_positions: { type: "integer" },` (OpenAPI schema for lead object)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create batch-12-fixes.test.js with failing positions/totalQty tests</name>
  <files>tests/batch-12-fixes.test.js</files>
  <read_first>
    - tests/email-analyzer.test.js (existing test style: node:test + node:assert, how analyzeEmail is invoked, how leads are constructed/inspected)
    - tests/p0-regression.test.js (regression test pattern for this file family)
    - src/services/email-analyzer.js lines 603-650 (analyzeEmail signature and how a payload is shaped)
    - src/services/email-analyzer.js line 5746 (normalizeArticleCode behavior, so test article codes dedup correctly)
    - .planning/phases/12-quantity-and-inn/12-VALIDATION.md (per-task verification map)
  </read_first>
  <behavior>
    - Test (Belgormash): a lead whose lineItems array contains 18 entries that normalize to exactly 2 unique article codes, each unique code having qty 5 → after finalizeLeadCounts, lead.positions === 2 and lead.totalQty === 10.
    - Test (dedup picks max qty): two lineItems with the SAME normalized code but qty 3 and qty 7 → that position contributes 7 to totalQty (not 10, not 3).
    - Test (null qty as 0): a lead with 2 unique codes where one lineItem has quantity null → that position contributes 0; totalQty = sum of the others; positions still counts it.
    - Test (no articles): a lead with empty articles and empty lineItems → lead.positions === 0 (strict, not undefined) and lead.totalQty === 0.
    - Test (article without lineItem): an article code present in lead.articles but with no matching lineItem → counts toward positions with qty 0.
  </behavior>
  <action>
    Create `tests/batch-12-fixes.test.js`. Import `finalizeLeadCounts` from `../src/services/email-analyzer.js` (Task 2 will add the `export` keyword to it).

    Write the five tests described in `<behavior>` using `node:test` (`test`/`describe`) and `node:assert/strict`, matching the import and assertion style of `tests/email-analyzer.test.js`. Construct plain `lead` objects directly (do NOT route through full analyzeEmail) — e.g.:
    ```javascript
    import { test } from "node:test";
    import assert from "node:assert/strict";
    import { finalizeLeadCounts } from "../src/services/email-analyzer.js";

    test("ART-04 Belgormash: 18 dup lineItems → positions 2, totalQty 10", () => {
        const dup = (code, qty) => ({ article: code, quantity: qty });
        const lead = {
            articles: ["MD-025-6L", "AB 100"],
            lineItems: [
                ...Array(9).fill(dup("MD-025-6L", 5)),
                ...Array(9).fill(dup("AB 100", 5)),
            ],
        };
        finalizeLeadCounts(lead);
        assert.equal(lead.positions, 2);
        assert.equal(lead.totalQty, 10);
    });
    ```
    Add a top-of-file comment `// Phase 12 regression — ART-04 (positions/totalQty) + CONTACT-02 (INN checksum, added in plan 02)`.
    These tests MUST fail right now (the function does not exist / is not exported yet) — that is the RED state. Commit with message `test(12-01): add failing positions/totalQty tests for ART-04`.
  </action>
  <verify>
    <automated>node tests/batch-12-fixes.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/batch-12-fixes.test.js` exists.
    - `tests/batch-12-fixes.test.js` contains the string `finalizeLeadCounts`.
    - `tests/batch-12-fixes.test.js` contains an assertion `assert.equal(lead.positions, 2)` (or equivalent expected value 2 for positions) and an assertion expecting `totalQty` value `10`.
    - `tests/batch-12-fixes.test.js` contains a test asserting `lead.positions` strictly equals `0` for the no-articles case.
    - Running `node tests/batch-12-fixes.test.js` FAILS (tests are red because finalizeLeadCounts is not yet implemented/exported).
  </acceptance_criteria>
  <done>Test file exists with 5 tests covering Belgormash, max-qty dedup, null-qty, no-articles, and article-without-lineItem; suite currently RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement finalizeLeadCounts() and wire into analyzeEmail</name>
  <files>src/services/email-analyzer.js, .railway-deploy/src/services/email-analyzer.js</files>
  <read_first>
    - src/services/email-analyzer.js line 254 (normalizeInn — shows local helper-function placement style)
    - src/services/email-analyzer.js line 349 (validateSenderFields — canonical post-processing function pattern to mirror)
    - src/services/email-analyzer.js line 5746 (normalizeArticleCode — exact normalization to reuse as dedup key)
    - src/services/email-analyzer.js lines 1795-1815 (the analyzeEmail finalization area where validateSenderFields is called at 1807)
    - tests/batch-12-fixes.test.js (the failing tests this task must turn green)
  </read_first>
  <action>
    In `src/services/email-analyzer.js`:

    1. Add and EXPORT a new function `finalizeLeadCounts(lead)` near the other lead helpers (place it just below `validateSenderFields` or below `normalizeArticleCode` — keep it co-located with lead helpers, 4-space indent, camelCase). Implementation:
    ```javascript
    export function finalizeLeadCounts(lead) {
        if (!lead) return lead;
        const seen = new Map(); // normalizedCode -> max qty seen
        for (const item of (lead.lineItems || [])) {
            const code = normalizeArticleCode(item && item.article ? item.article : "").toLowerCase();
            if (!code) continue;
            const qty = (item.quantity != null) ? Number(item.quantity) : 0;
            const safeQty = Number.isFinite(qty) ? qty : 0;
            const current = seen.get(code);
            if (current === undefined || safeQty > current) {
                seen.set(code, safeQty);
            }
        }
        for (const a of (lead.articles || [])) {
            const code = normalizeArticleCode(a || "").toLowerCase();
            if (code && !seen.has(code)) seen.set(code, 0);
        }
        lead.positions = seen.size;
        lead.totalQty = [...seen.values()].reduce((sum, q) => sum + q, 0);
        return lead;
    }
    ```

    2. Wire it into `analyzeEmail()`: immediately AFTER line 1807 (`const senderCorrections = validateSenderFields(sender);`) add a call `finalizeLeadCounts(lead);`. The `lead` variable is in scope in that block (it is the same `lead` referenced at line 1796). This is the single authoritative write — the seven existing `lead.totalPositions = Math.max(...)` assignments (lines 1059, 1089, 1134, 1448, 1562, 1746, 1734-area synth, plus 3064/3183 in the alternate merge path) REMAIN unchanged as intermediate trackers; finalizeLeadCounts overwrites the final values.

    3. Keep `lead.totalPositions` for backward compat by setting `lead.totalPositions = lead.positions;` as the last line inside finalizeLeadCounts BEFORE the `return` — so any reader still using the old in-memory name (e.g. the requestSize helper at line 3250 `if ((lead.totalPositions || 0) >= 5)` and the diagnostics at line 6376) gets the corrected value rather than the stale Math.max value.

    Run the test file — all 5 tests must pass (GREEN). Then run the full suite to confirm no regression.
    Commit: `feat(12-01): add finalizeLeadCounts for ART-04 positions/totalQty`.
  </action>
  <verify>
    <automated>node tests/batch-12-fixes.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/services/email-analyzer.js` contains `export function finalizeLeadCounts(lead)`.
    - `src/services/email-analyzer.js` contains `finalizeLeadCounts(lead);` within the analyzeEmail finalization block (grep shows it AFTER the validateSenderFields call, before `return result`).
    - finalizeLeadCounts body contains `lead.positions = seen.size;` and `lead.totalQty = [...seen.values()].reduce`.
    - finalizeLeadCounts sets `lead.totalPositions = lead.positions;` before return.
    - `node tests/batch-12-fixes.test.js` exits 0 (all 5 tests pass).
    - `npm test` shows no NEW failures beyond the documented pre-existing failures (3 pre-existing: docx/xlsx/company-directory per MEMORY.md).
  </acceptance_criteria>
  <done>finalizeLeadCounts implemented and called once after validateSenderFields; Belgormash test green; full suite shows only pre-existing failures.</done>
</task>

<task type="auto">
  <name>Task 3: Rename total_positions consumers to positions + add total_qty, mirror to .railway-deploy</name>
  <files>src/services/integration-api.js, src/services/crm-adapters.js, src/services/integration-openapi.js, src/services/llm-extractor.js, .railway-deploy/src/services/email-analyzer.js, .railway-deploy/src/services/integration-api.js, .railway-deploy/src/services/crm-adapters.js, .railway-deploy/src/services/llm-extractor.js</files>
  <read_first>
    - src/services/integration-api.js lines 135-150 (serialization of lead to snake_case storage form)
    - src/services/crm-adapters.js lines 128-170 (both COMMENTS line 133 and build1CPayload line 167)
    - src/services/integration-openapi.js line 960-968 (lead schema block)
    - src/services/llm-extractor.js line 325-332 (where totalPositions is computed by the LLM path)
    - .railway-deploy/src/services/email-analyzer.js (target mirror — confirm it currently matches src/ before editing)
  </read_first>
  <action>
    Per D-01, propagate the rename so the deduplicated value reaches storage and consumers, and add `total_qty`.

    1. `src/services/integration-api.js` line 141: replace
       `total_positions: lead.totalPositions || 0,`
       with two lines:
       `positions: lead.positions || 0,`
       `total_qty: lead.totalQty || 0,`
       (This is the serialization boundary; storage will now persist `positions` and `total_qty`.)

    2. `src/services/crm-adapters.js` (reads STORED snake_case):
       - line 133: replace `` `Позиций: ${lead.total_positions || 0}`, `` with `` `Позиций: ${lead.positions || 0}`, `` and add a following array element `` `Всего шт.: ${lead.total_qty || 0}`, `` .
       - line 167: replace `totalPositions: lead.total_positions || 0,` with `positions: lead.positions || 0,` and add `totalQty: lead.total_qty || 0,` on the next line.

    3. `src/services/integration-openapi.js` line 964: replace `total_positions: { type: "integer" },` with two lines:
       `positions: { type: "integer" },`
       `total_qty: { type: "integer" },`

    4. `src/services/llm-extractor.js` line 329: after the existing `result.lead.totalPositions = ...` line, the in-memory value will be overwritten by finalizeLeadCounts downstream — leave the line but ALSO ensure the LLM path benefits: no change strictly required here, but add `result.lead.positions = result.lead.totalPositions;` immediately after line 329 so the field name exists even if finalizeLeadCounts is skipped on an LLM-only path. (4-space indent, matching the file.)

    5. Mirror ALL edits from this plan (Tasks 2 and 3) to `.railway-deploy/src/services/`:
       - copy `src/services/email-analyzer.js` → `.railway-deploy/src/services/email-analyzer.js`
       - copy `src/services/integration-api.js` → `.railway-deploy/src/services/integration-api.js`
       - copy `src/services/crm-adapters.js` → `.railway-deploy/src/services/crm-adapters.js`
       - copy `src/services/llm-extractor.js` → `.railway-deploy/src/services/llm-extractor.js`
       (integration-openapi.js is NOT present in .railway-deploy per the file listing — skip mirroring it; verify with a file-existence check before copying and skip if absent.)

    After edits, run the full suite. Commit: `refactor(12-01): rename total_positions to positions, add total_qty, mirror to railway-deploy`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/services/integration-api.js` contains `positions: lead.positions || 0,` and `total_qty: lead.totalQty || 0,`.
    - `src/services/integration-api.js` no longer contains `total_positions: lead.totalPositions`.
    - `src/services/crm-adapters.js` contains `lead.positions` and `lead.total_qty` (grep both).
    - `src/services/integration-openapi.js` contains `positions: { type: "integer" }` and `total_qty: { type: "integer" }`.
    - `.railway-deploy/src/services/email-analyzer.js` contains `export function finalizeLeadCounts`.
    - `.railway-deploy/src/services/integration-api.js` contains `positions: lead.positions || 0,`.
    - SHA256 of `src/services/email-analyzer.js` equals SHA256 of `.railway-deploy/src/services/email-analyzer.js` (mirror is exact).
    - `npm test` shows only the 3 documented pre-existing failures.
  </acceptance_criteria>
  <done>All consumers read positions/total_qty; src/ and .railway-deploy/src/ mirrors are byte-identical for the touched files; suite green except pre-existing.</done>
</task>

</tasks>

<verification>
- `node tests/batch-12-fixes.test.js` passes (5 ART-04 tests).
- `npm test` shows only the 3 known pre-existing failures (docx/xlsx/company-directory).
- `grep -rn "total_positions: lead.totalPositions" src/` returns nothing (rename complete at serialization boundary).
- `finalizeLeadCounts(lead)` appears exactly once in `analyzeEmail()` after `validateSenderFields`.
- src/ and .railway-deploy/src/ mirrors of email-analyzer.js, integration-api.js, crm-adapters.js, llm-extractor.js are byte-identical.
</verification>

<success_criteria>
ART-04 satisfied: for a lead with duplicated lineItems, `positions` = unique normalized article-code count and `totalQty` = sum of max qty per unique code, with null qty counting as 0 and positions = 0 when no articles. The Belgormash regression (18/7 → 2/10) is green. The deduplicated values reach the CRM/integration payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/12-quantity-and-inn/12-01-SUMMARY.md`
</output>
