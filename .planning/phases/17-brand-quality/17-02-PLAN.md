---
phase: 17-brand-quality
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - scripts/audit_baseline.py
  - scripts/baselines/baseline_v8.json
autonomous: false
requirements: [BRAND-06]
user_setup: []
must_haves:
  truths:
    - "The audit's brand grounding check uses the full body field when available, not only the 600-char bodyPreview (GHOST-5 measurement fix)"
    - "Phase 17 code fixes are deployed to Railway production (.railway-deploy/src/ live)"
    - "Production messages are reanalyzed so detectedBrands reflect the new filters"
    - "baseline_v8.json captured on the same 300-email seed=42 sample shows brand.noise_free ≥ 0.55"
  artifacts:
    - path: "scripts/audit_baseline.py"
      provides: "check_brand uses full body when present (GHOST-5 fix)"
      contains: "msg.get(\"body\")"
    - path: "scripts/baselines/baseline_v8.json"
      provides: "Post-fix baseline with brand.noise_free measurement"
      contains: "brand"
  key_links:
    - from: "scripts/audit_baseline.py check_brand"
      to: "is_brand_grounded full-body source"
      via: "body = msg.get('body') or body_preview"
      pattern: "msg.get\\(\"body\"\\)"
    - from: "Railway production"
      to: ".railway-deploy/src/services brand fixes"
      via: "deploy + reanalysis"
      pattern: "baseline_v8"
---

<objective>
Make the brand-noise measurement faithful (GHOST-5), ship the Plan 01 fixes to production, reanalyze, and capture `baseline_v8.json` proving `brand.noise_free ≥ 55%` (BRAND-06).

GHOST-5 is a measurement artifact, not a detection bug: the audit's `check_brand` reads `msg.get("bodyPreview")` (max 600 chars), so legitimate brands appearing after char 600 are counted as ghosts. 248 of 294 ghost-brand emails had truncated previews. Fixing this makes the metric accurate without changing production code.

Purpose: Prove the milestone goal (brand.noise_free ≥ 55%) on the same seed=42 / 300-email sample used for baseline_v7, and ship the code fixes to managers.
Output: `scripts/audit_baseline.py` updated + `scripts/baselines/baseline_v8.json` captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-brand-quality/17-RESEARCH.md
@.planning/phases/17-brand-quality/17-VALIDATION.md
@scripts/baselines/baseline_v7.json

<interfaces>
<!-- Exact contracts extracted from scripts/audit_baseline.py -->

check_brand — scripts/audit_baseline.py lines 349-363 (CURRENT CODE):
```python
def check_brand(msg, kb_a, kb_s, aliases):
    a = msg.get("analysis") or {}
    l = a.get("lead") or {}
    brands = brand_names(a.get("detectedBrands") or l.get("detectedBrands") or [])
    if not brands:
        return {"present": False, "noise": False}
    body = msg.get("bodyPreview") or ""
    subj = msg.get("subject") or ""
    att = ((a.get("attachmentAnalysis") or {}).get("combinedText") or "")
    arts = article_codes(l.get("articles"))
    any_ghost = any(
        not is_brand_grounded(b, body, subj, att, arts, kb_a, kb_s, aliases)
        for b in brands
    )
    return {"present": True, "noise": any_ghost}
```

baseline_v7 brand block (target to beat):
```json
"brand": { "present": 0.6, "noise_free": 0.4533, "n": 300 }
```

AUDIT INVOCATION (from 17-VALIDATION.md Manual-Only Verifications):
```
python scripts/audit_baseline.py --token $TOKEN --limit 300 --seed 42 --skip-n8n --out scripts/baselines/baseline_v8.json
```
NOTE: same seed=42 + limit=300 reproduces the exact baseline_v7 sample. Constants in audit_baseline.py: DEFAULT_SAMPLE=300, DEFAULT_SEED=42, BASE="https://pochta-production.up.railway.app".

OPEN QUESTION (17-RESEARCH.md #2): the live API may return only `bodyPreview`, not `body`. The fix MUST be defensive: `body = msg.get("body") or body_preview`. If `body` is absent, behavior is unchanged (no regression). The reanalysis-driven detection improvement (Plan 01) is the primary driver toward ≥55%; the GHOST-5 audit fix is a secondary, safe correction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GHOST-5 — audit check_brand uses full body when available</name>
  <files>scripts/audit_baseline.py</files>
  <read_first>
    - scripts/audit_baseline.py lines 349-363 (check_brand function)
    - scripts/audit_baseline.py lines 172-206 (is_brand_grounded — confirm it takes body as first text arg)
    - .planning/phases/17-brand-quality/17-RESEARCH.md "Pattern 5" and "Audit Script Fix P5" code example
  </read_first>
  <action>
Edit `scripts/audit_baseline.py`, function `check_brand` (lines 349-363). Replace the single body source line `body = msg.get("bodyPreview") or ""` with a defensive full-body-preferred source:
```python
    body_preview = msg.get("bodyPreview") or ""
    # GHOST-5 / Phase 17: bodyPreview is capped at 600 chars; legitimate brands after
    # char 600 were counted as ghosts. Prefer full body if the snapshot/API provides it.
    body = msg.get("body") or body_preview
```
The existing call `is_brand_grounded(b, body, ...)` already passes `body` — leave it. Leave subject, att, arts unchanged. This is a measurement-only change — do NOT touch any production JS.

Do NOT mirror this to .railway-deploy — it is a Python audit script, not deployed runtime code.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/audit_baseline.py', encoding='utf-8').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep `msg.get("body") or body_preview` present in scripts/audit_baseline.py
    - grep `body = msg.get("bodyPreview") or ""` returns NO matches inside check_brand (old single-source line replaced)
    - `python -c "import ast; ast.parse(open('scripts/audit_baseline.py', encoding='utf-8').read())"` exits 0 (valid Python)
    - The comment `GHOST-5` appears in scripts/audit_baseline.py
  </acceptance_criteria>
  <done>Audit grounds brands against full body when available; falls back to bodyPreview with no regression when body absent.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy Plan 01 fixes to Railway + trigger production reanalysis</name>
  <files>.railway-deploy/src/services/email-analyzer.js, .railway-deploy/src/services/detection-kb.js, .railway-deploy/src/services/brand-normalizer.js</files>
  <read_first>
    - C:/Users/Staurus/.claude/projects/C--Opencode-test-pochta/memory/project_railway_deploy.md (deploy flow — deploys from .railway-deploy/src/)
    - C:/Users/Staurus/.claude/projects/C--Opencode-test-pochta/memory/reference_railway_access.md (Railway tokens, project/service IDs, redeploy + reanalysis recipes)
    - .planning/phases/17-brand-quality/17-01-SUMMARY.md (confirm which files were mirrored)
  </read_first>
  <action>
Confirm the Plan 01 edits are present in `.railway-deploy/src/services/email-analyzer.js`, `.railway-deploy/src/services/detection-kb.js`, and `.railway-deploy/src/services/brand-normalizer.js` (Plan 01 mirrored them). If git shows these mirror files as uncommitted, ensure they are committed.

Deploy to Railway using the recipe in `reference_railway_access.md` (Railway Project Token + redeploy via CLI/GraphQL). After deploy completes, run a smoke-check: `curl https://pochta-production.up.railway.app/api/health` returns 200 (per CLAUDE.md deployment-verification rule — NEVER declare deploy done without smoke-check).

Then trigger production reanalysis of the corpus so `detectedBrands` reflect the new filters. Use the reanalysis endpoint/recipe documented in reference_railway_access.md / project_railway_deploy.md (e.g. `POST /api/projects/project-3-mailbox-file/reanalyze` and the same for project-4, with the admin Bearer token). Reanalysis must complete (the same project-3 + project-4 messages the baseline samples) before baseline capture in Task 4.

Do NOT call any n8n send/resend endpoint (per memory feedback_no_production_api_without_request — reanalysis only, no /crm-resend).
  </action>
  <verify>
    <automated>curl -s -o NUL -w "%{http_code}" https://pochta-production.up.railway.app/api/health</automated>
  </verify>
  <acceptance_criteria>
    - `curl https://pochta-production.up.railway.app/api/health` returns HTTP 200
    - The three .railway-deploy/src/services brand files contain the Phase 17 markers (grep `length > 0 && quotedContent`, `GENERIC_SPLIT_PARTS`, the six GHOST-4 aliases) — i.e. the deployed code carries the fixes
    - Reanalysis run confirmed complete (run log / endpoint response shows processed count > 0)
    - No n8n send/resend endpoint was called
  </acceptance_criteria>
  <done>Production runs the Phase 17 brand fixes; health smoke-check green; corpus reanalyzed with new filters.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verify deploy live + reanalysis complete before baseline capture</name>
  <files>(none — verification checkpoint, no files modified)</files>
  <action>
This is a blocking human-verification checkpoint. Claude has already deployed and reanalyzed in Task 2; here Claude PAUSES and asks the user to confirm production state is correct before the irreversible baseline capture in Task 4. Claude does no further automation in this task — it presents the verification steps below and waits for the resume signal.
  </action>
  <what-built>
    Audit script GHOST-5 fix (full-body grounding), Plan 01 brand fixes deployed to Railway, and production corpus reanalyzed with the new filters.
  </what-built>
  <how-to-verify>
    1. Confirm Railway shows a fresh deploy (latest commit) and `curl https://pochta-production.up.railway.app/api/health` → 200.
    2. Confirm reanalysis finished (no in-progress jobs) before capturing the baseline — the audit reads live detection results, so a half-reanalyzed corpus would understate the gain.
    3. Provide / confirm the admin token to use for the audit run in Task 4 (or approve using the credentials in audit_baseline.py).
  </how-to-verify>
  <verify>
    <automated>curl -s -o NUL -w "%{http_code}" https://pochta-production.up.railway.app/api/health</automated>
  </verify>
  <resume-signal>Type "approved" to proceed to baseline capture, or describe issues (deploy not live / reanalysis incomplete / wrong token).</resume-signal>
  <done>User confirms deploy is live and reanalysis is complete; audit token confirmed for Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: Capture baseline_v8 + measure brand.noise_free delta vs baseline_v7</name>
  <files>scripts/baselines/baseline_v8.json</files>
  <read_first>
    - scripts/baselines/baseline_v7.json (prior values: brand.present=0.6, brand.noise_free=0.4533, seed=42, n=300)
    - scripts/audit_baseline.py lines 502-575 (main flow: same seed=42 / limit=300 reproduces the sample)
    - .planning/phases/17-brand-quality/17-VALIDATION.md Manual-Only Verifications (exact audit command)
  </read_first>
  <action>
Run the audit against live production with the SAME sample parameters as baseline_v7 so the comparison is apples-to-apples:
```
python scripts/audit_baseline.py --token <ADMIN_TOKEN> --limit 300 --seed 42 --skip-n8n --out scripts/baselines/baseline_v8.json
```
(If no token is supplied, the script auto-logs in via the admin credentials baked into audit_baseline.py.)

After it writes `scripts/baselines/baseline_v8.json`, read the `fields.brand` block and compare to baseline_v7:
- baseline_v7: brand.noise_free = 0.4533
- target: baseline_v8 brand.noise_free ≥ 0.55

Record the delta (v8 − v7) in pp. Note in the summary which portion is attributable to the detection fixes (Plan 01) vs the GHOST-5 measurement correction.

If brand.noise_free < 0.55 after this run: do NOT silently pass. Report the shortfall, the residual ghost count, and the most common residual pattern (from the audit). Per 17-RESEARCH.md Open Question #3, this signals KB-wide slash-canonical cleanup may be needed — flag it for a gap-closure plan rather than fabricating the metric.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('scripts/baselines/baseline_v8.json', encoding='utf-8')); b=d['fields']['brand']['noise_free']; print(f'brand.noise_free={b}'); assert b >= 0.55, f'BELOW TARGET: {b} < 0.55'"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/baselines/baseline_v8.json` exists with `sample_seed`=42 and `sample_size`=300
    - `fields.brand.noise_free` ≥ 0.55 in baseline_v8.json
    - `fields.brand.noise_free` (v8) > 0.4533 (v7) — measurable improvement recorded
    - Delta (v8 − v7) in pp is documented in the summary
    - The verify command exits 0 (assertion passes)
  </acceptance_criteria>
  <done>baseline_v8.json captured on seed=42/n=300; brand.noise_free ≥ 55%; delta vs v7 documented. (If below target, shortfall + residual pattern reported and gap-closure flagged — not masked.)</done>
</task>

</tasks>

<verification>
- `python -c "import ast; ..."` confirms audit_baseline.py is valid Python after GHOST-5 edit
- `curl .../api/health` → 200 confirms deploy live
- `baseline_v8.json` `fields.brand.noise_free` ≥ 0.55 (BRAND-06 satisfied)
- Delta v8 vs v7 documented; residual flagged if target missed
</verification>

<success_criteria>
- GHOST-5 audit measurement fix landed in scripts/audit_baseline.py (full body preferred, bodyPreview fallback)
- Plan 01 fixes deployed to Railway; health smoke-check green; corpus reanalyzed
- baseline_v8.json captured on the same seed=42/n=300 sample as baseline_v7
- brand.noise_free ≥ 0.55 (target met) OR shortfall + residual pattern reported with gap-closure flag
- No n8n send/resend endpoints called
</success_criteria>

<output>
After completion, create `.planning/phases/17-brand-quality/17-02-SUMMARY.md` documenting: the GHOST-5 audit change, deploy + reanalysis confirmation (health 200 + reanalysis count), baseline_v8 brand.noise_free value, the delta vs baseline_v7 in pp, and whether the ≥55% target was met.
</output>
