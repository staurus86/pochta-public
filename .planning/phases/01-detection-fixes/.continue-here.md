---
phase: 01-detection-fixes
task: 5
total_tasks: 8
status: in_progress
last_updated: 2026-04-10T20:49:03.739Z
---

<current_state>
Сессия 10.04.2026. Работаем над системными фиксами пайплайна детекции.

Сделали 3 патча (commits 67f2ef2, f00079e, 657213f), задеплоили на Railway, 
запустили переанализ 1790 писем, создали pochta-analysis-500-v2.xlsx.

Пользователь провёл аудит v2 → 53 ошибки из 500 писем.
Пользователь сказал: "нужен рабочий детект, не точечные правки — сделай кардинальные фиксы".

Начали планировать системные фиксы, но контекст исчерпался (92%).
</current_state>

<completed_work>

- Task 1: Fix company field (43 ошибки) — Done → commit 67f2ef2
  - sanitizeCompanyName: INN-prefix, PDF_COMPANY_NOISE_TOKENS, телефон/адрес/должность, bare ООО
  - extractCompanyFromLabels: skip ИНН: prefix

- Task 2: Fix classification (14 ошибок) — Done → commit f00079e
  - robot form non-resume → override Клиент
  - 8 новых sender profiles: obed.ru, elecrow.com, tektorg.ru, 1c-uc.ru, cdek.ru, slacnc.com, eayglobal.com, globalpost.ru
  - Правило: ООО/АО с кавычками в теле → +3 client score

- Task 3: Fix sender name (6 ошибок) — Done → commit 657213f
  - Паттерн "Фамилия И.В." (surname + initials)
  - KB signature_hint: брать только первую строку (fix multiline)
  - Single surname в structured block

- Task 4: Deploy + reanalysis + xlsx — Done
  - git push → Railway deploy OK
  - Переанализ: 1771/1790
  - Создан pochta-analysis-500-v2.xlsx (500 строк, тот же формат)
  - Итоги: Клиент 1523, СПАМ 246, Поставщик 14, Не определено 7
</completed_work>

<remaining_work>

- Task 5 (ВЫСОКИЙ): Fix — email из quoted history → own domain [НЕ НАЧАТА]
  ФАЙЛ: src/services/email-analyzer.js, строка 328
  ПРОБЛЕМА: extractForwardedSender видит "From: SIDERUS [mailto:info@siderus.ru]" в quoted части
  и переопределяет fromEmail → info@siderus.ru. 13 строк в аудите затронуты.
  ФИКС: строка 328 — добавить условие `&& !isOwnDomain(fwdInfo.email.split("@")[1])`
  
  Текущий код:
    if (fwdInfo.email && !fromEmail.includes(fwdInfo.email.split("@")[1])) {
      fromEmail = fwdInfo.email;
  Нужно:
    if (fwdInfo.email && !fromEmail.includes(fwdInfo.email.split("@")[1]) && !isOwnDomain(fwdInfo.email.split("@")[1])) {
      fromEmail = fwdInfo.email;

- Task 6 (ВЫСОКИЙ): Fix — bodyForClassification от quoted-history spam [НЕ НАЧАТА]
  ПРОБЛЕМА: Красмаш (row 107), Элакс (329), ФолгГрупп (404), Демар-Центр (490) → СПАМ
  Причина: их короткий реальный текст + спам-слова из quoted части Siderus письма
  ФИКС 1: если primaryBody.trim().length < 50 — брать body.slice(0, 500) вместо полного body
  ФИКС 2: добавить в QUOTE_PATTERNS Outlook/Exchange форматы которые не покрыты:
    - "________" (underscores separator)
    - "From: X Sent: Y To: Z" (Outlook inline)
    - "Sent from Outlook"
  ФАЙЛ: src/services/email-analyzer.js, строки 112-120 (QUOTE_PATTERNS) и 348 (bodyForClassification)

- Task 7 (ВЫСОКИЙ): Fix — sender profiles в продакшн DB [НЕ НАЧАТА]
  ПРОБЛЕМА: 8 новых доменов не попали в prod DB (seeding WHERE NOT EXISTS пропустил)
  Строки: Обед.ру (1,81), ТЭК-Торг (244), 1С (265), CDEK (27), EAY Global (96), ГлобалПост (187)
  ФИКС: добавить через API:
    TOKEN=eyJzdWIiOjEsImxvZ2luIjoiYWRtaW4i... (в project_auth_gate.md)
    POST https://pochta-production.up.railway.app/api/detection-kb/sender-profiles
    Body: { "senderEmail": "", "senderDomain": "obed.ru", "classification": "spam", "notes": "..." }
  ЛИБО: добавить в initialize() upsert-логику вместо WHERE NOT EXISTS

- Task 8 (СРЕДНИЙ): Fix — extended company noise [НЕ НАЧАТА]
  ПРОБЛЕМА: "United Process Co", "Any co", "SPecialiST RePack AppVersion Co" — мусор
  ФИКС: расширить PDF_COMPANY_NOISE_TOKENS + добавить проверки:
    - reject если слово + " co" и base-слово короткое/generic (any, some, process, united, dust)
    - reject если содержит software-паттерны (AppVersion, RePack, SpecialiST)
    - reject "Snipermail" (free email сервис)
    - Проверить почему 15 строк ИНН: всё ещё проходят (возможно format "Наш ИНН: XXXX" без ^ в начале)
</remaining_work>

<decisions_made>

- robot form non-resume → всегда override в Клиент (форма на сайте = клиентский канал)
- PDF_COMPANY_NOISE_TOKENS как Set (быстро + легко расширять)
- Системный фикс вместо точечных — пользователь явно запросил
- Sender profiles через DEFAULT_SENDER_PROFILES seeding — НО: не работает для существующей prod DB
</decisions_made>

<blockers>

- Sender profiles в prod DB: WHERE NOT EXISTS seeding пропустил новые домены.
  Решение: добавить через API (POST /api/detection-kb/sender-profiles) или migration
</blockers>

<context>
Пользователь (Staurus, проект pochta-platform) хочет системный рабочий детект, не патчи.
Клиент компании: Siderus / Коловрат (промышленная торговля).
Продакшн: https://pochta-production.up.railway.app/ | admin/LgxaZ@ZDgNBXgSpnmTHEW6MC
Pre-existing failures (не трогать): docx/xlsx/company-directory (3 теста).

Главная системная проблема: quoted history в email-цитатах contaminate всё — email, компанию, 
классификацию, бренды. Нужно агрессивнее резать quoted части.
</context>

<next_action>
1. Читать src/services/email-analyzer.js строки 326-330 (extractForwardedSender override)
2. Добавить isOwnDomain check — одна строка кода, большой эффект
3. Затем расширить QUOTE_PATTERNS (строки 112-120)
4. Затем добавить sender profiles через API curl-командами
5. Деплой + переанализ + новый xlsx v3
</next_action>
